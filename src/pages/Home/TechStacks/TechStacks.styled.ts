import { Chip, styled, Typography } from "@mui/material";

export const TechStackItem = styled(Chip)({
  margin: "4px 8px 4px 0",
});

export const TechStackLabel = styled(Typography)({
  textTransform: "capitalize",
  fontWeight: 600,
});
