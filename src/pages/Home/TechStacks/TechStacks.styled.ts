import { Box, Chip, styled } from "@mui/material";

export const TechStackCategoryWrapper = styled(Box)({
  display: "flex",
});

export const TechStackItem = styled(Chip)({
  margin: "4px 8px 4px 0",
});
